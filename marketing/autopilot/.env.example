# Copy to .env (gitignored). Postiz public-API access for publish.mjs / pull-analytics.mjs.
POSTIZ_URL=https://social.sendemall.com
# Postiz UI -> Settings -> Public API -> generate. NOT the same as the old instance's key.
POSTIZ_API_KEY=
